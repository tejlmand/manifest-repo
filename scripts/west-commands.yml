# Keep the help strings in sync with the values in the .py files!
west-commands:
  - file: scripts/west-commands/my_extension.py
    commands:
      - name: my-command
        class: MyCommand
        help: display shell completion scripts
